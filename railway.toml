[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && node railway-build.mjs"

[deploy]
startCommand = "node railway-start.mjs"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
