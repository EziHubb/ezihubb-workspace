[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install -g pnpm@11.0.9",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["node railway-build.mjs"]

[start]
cmd = "node railway-start.mjs"
