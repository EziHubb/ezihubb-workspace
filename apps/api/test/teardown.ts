export default async function globalTeardown() {
  // Intentionally empty — test DB is preserved between runs for inspection.
  // Add cleanup logic here if you want a clean slate after every suite.
}
