// Promoted to `@ezihubb/ui` (libs/ui/src/lib/components/Toggle/Toggle.tsx) so
// admin and client share one implementation. Re-exported here so existing
// imports across the product editor don't need to change.
export { Toggle, ToggleRow } from '@ezihubb/ui';
export type { ToggleProps, ToggleRowProps } from '@ezihubb/ui';
